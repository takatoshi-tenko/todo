<div id="<%= dom_id todo %>">
  <p>
    <strong>Title:</strong>
    <%= todo.title %>
    <br>
    <strong>Name:</strong>
    <%= todo.user %>

    <br>
    <strong>Date:</strong>
    <%= todo.updated_at.strftime('%Y/%m/%d %H:%M:%S') %>
  </p>

</div>
